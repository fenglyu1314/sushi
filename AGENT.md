# Agent.md — AI 行为规范

> 本文件是本项目（寿司日料经营模拟）中 AI 协作的**行为准则**。所有 AI 在本仓库内工作时须遵循本文件。
> 与项目设计/技术文档的关系：`docs/` 是设计事实来源，本文件只规定「AI 该如何工作」，不重复设计内容。

---

## 1. 项目认知

- **项目**：寿司日料经营模拟独立游戏（决策/规划向，非操作向）。核心 Hook = 浪费经济（做多卖不掉就倒掉亏钱）。
- **引擎**：Godot 4.7.1，2D 像素风。
- **开发模式**：单人 + AI 辅助，白盒优先（先验证"好不好玩"，再做正式美术）。
- **版本管理**：GitHub，约定式提交，`master` 稳定、功能用 `feature/*` 分支。
- **规范工作流**：OpenSpec（spec-driven），每个变更走 propose → apply → archive。

### 必读文档（按需查阅，不要凭空猜测设计）
| 文档 | 用途 |
|------|------|
| `docs/00-术语表.md` | **术语唯一裁决标准**，分歧时以此为准 |
| `docs/01-游戏设计文档-GDD.md` | 设计层稳定参考 |
| `docs/02-技术架构.md` | 技术实现约定 |
| `docs/03-开发路线图.md` | 里程碑 M0~M5 计划 |
| `openspec/config.yaml` | OpenSpec 项目上下文 |
| `BACKLOG.md` | 轻量任务收集箱（想法/待修 bug），见 §4.5 |

---

## 2. 术语纪律

- **一切命名、讨论、代码、文档用词以 `docs/00-术语表.md` 为唯一标准**。出现分歧，以术语表为裁决依据。
- 易混点（务必区分）：
  - **好感度** vs **热度**：两套独立指标，互不换算。
  - **流失**（机会成本，不扣钱） vs **浪费/作废**（实亏，扣钱）。
  - **食材保质期**（原料） vs **成品新鲜度**（传送带上转圈时限）。
  - **冰箱**（摆摊主料容器，占格） vs **货仓**（开店食材总仓，单一容量池）。
  - **确认情报**（可信） vs **小道消息**（概率性，可能失真）。
- 阶段编号：设计侧用 ①②③④⑤，开发侧用 M0~M5；二者不混用。

---

## 3. 关键禁令

### 3.1 不直接创建/修改 Godot 引擎文件
- **禁止**直接创建或编辑：`.tscn`（场景）、`.tres`（资源）、`project.godot`、`.import` 等。
- **原因**：绕过 Godot 编辑器会破坏引擎的导入、UID 分配/管理、资源依赖刷新等自动化。
- **正确做法**：在编辑器内操作；或仅向用户说明操作步骤。普通脚本（`.cs`/`.gd`）不受此限。

### 3.2 不擅自拍板未决项（📝待定）
- 编程语言与数据格式已确认（GDScript + Godot Resource，见 `02-技术架构.md`）。
- 其他标 🔴/❓ 的决策尚未确定时，在用户确认前**不要假定**某一方案；涉及这些区域的代码须先与用户确认。

### 3.3 遇文档矛盾/不一致须停下询问，不擅自决策
- 在阅读文档、写代码、讨论设计时，若发现**任两处文档之间**、**文档与代码之间**、或**文档与用户当前表述之间**存在矛盾或不一致，**必须停下来向用户指出并询问**，不得擅自选择一方执行。
- **术语表优先级最高**：`docs/00-术语表.md` 是全项目术语的唯一裁决标准。任何地方（GDD、技术架构、代码命名、讨论等）与术语表冲突时，以术语表为准；但**不得自行修改其他文档去"对齐"**，应先向用户报告冲突点，由用户裁决如何处理。
- 报告冲突时：① 指出具体出处（哪个文档哪一节/哪行）；② 说明两方的差异；③ 给出建议但等用户确认后再行动。

### 3.4 不主动创建文档/文件
- 除非用户明确要求，不主动创建 `README`、`*.md` 等文档文件。
- 改动聚焦于用户当前请求，不做无关重构。

### 3.5 操作边界：文本/代码由 AI 落地，引擎/视觉由用户手动操作
- **AI 主责（直接落地）**：文本与代码层面的工作——脚本逻辑（`.gd`/`.cs`）、文档、设计讨论、数据结构与算法等。
- **交由用户手动操作（AI 只给操作建议与分步指导，不直接改文件）**：
  - **引擎内操作**：场景搭建、节点树/信号连接、Autoload 注册、运行调试等需在 Godot 编辑器内完成的操作。
  - **视觉/美术资源**：美术素材的导入与处理、精灵/图集/动画等。
  - **UI 调整**：布局、锚点、主题、控件属性等在编辑器内的可视化调整。
  - **配置修改**：`project.godot`、`.import`、`.tres` 等引擎配置/资源文件（与 §3.1 一致）。
- **给建议的方式**：分步说明「在编辑器哪里操作、改哪个值、为什么」，必要时指出对应文件与字段，但由用户落地确认。

---

## 4. 协作准则

### 4.1 基于事实，不臆造
- 涉及设计/数值/规则时，**先查 `docs/` 对应文档**，不凭记忆或常识编造。
- 文档未覆盖的待调数值（标 ❓/🔴），明确标注"待定"并询问用户，不填占位假数据。

### 4.2 最小改动，聚焦任务
- 做用户要求的事，不多做、不少做。
- 优先 `replace_in_file` 做针对性编辑；不重写用户的大文件除非必要。
- 改动须可立即运行：补全 import/依赖，不留半成品。

### 4.3 数据驱动意识
- 内容（食材/配方/顾客/事件）应为**数据资源**，逻辑与数据分离，目标"加内容不改代码"。
- 新增内容时考虑走数据而非硬编码（具体格式待 🔴 决策后统一）。

### 4.4 白盒优先
- 美术用纯色矩形/圆形 + 文字标签占位，固定网格（16×16 或 32×32）。
- 不提前做正式美术；保持"占位可读性"。

### 4.5 任务收集箱（BACKLOG）与升级为 change 的时机
- `BACKLOG.md` 是**未成形想法的收集箱**，定位区别于 OpenSpec：BACKLOG 记「快速捕获、尚未规划」的想法与待修 bug；OpenSpec change 记「已决定要做、需成规模开发」的功能。二者职责不重叠。
- **写入约定**：只 append 一行，格式 `- [ ] (YYYY-MM-DD) [可选里程碑/模块] 描述`；分类记入「💡 想法/待办」或「🐛 待修 Bug」。
- **两个典型场景**：
  - 开发中忽然想到要做的事 → 记入 BACKLOG「想法/待办」，不打断当前任务。
  - 与当前工作强绑定的代码问题 → 就地写 `# TODO/FIXME` 注释（编辑器可聚合）；如需跟进，同时在 BACKLOG「待修 Bug」引一笔。
- **升级为 change**：当某条目成熟、值得正式开发时，走 OpenSpec propose 流程立一个 change；随后在 BACKLOG 把该条目移入「✅ 已完成 / 已转正式」并标注对应 change 名。
- AI 不擅自增删 BACKLOG 条目；仅在用户明确要求时代为记录或整理。

---

## 5. 代码与架构约定

- **架构分层**：数据层(Resource) → 模拟层(纯逻辑) → 表现层(节点/UI)；模拟层尽量不依赖节点树，便于测试。
- **通信**：全局单例(Autoload) 事件总线 + 信号解耦。
- **时间模型**：一天为一局，内部 tick 固定步长推进（决策阶段 → 模拟阶段 → 结算）。
- **目录结构**：遵循 `docs/02-技术架构.md` §5（`scenes/`、`scripts/{core,systems,entities,ui}/`、`data/`、`assets/{whitebox,art}/`）。
- **提交**：约定式提交（feat/fix/docs/refactor…）。

---

## 6. 沟通风格

- 简洁、直接、切题；用中文。
- 引用代码用 ``起始行:结束行:文件路径`` 格式。
- 不加无谓的解释性注释；不滥用 emoji。
- 遇到不确定先查文档/搜索，再决定是否询问用户。

---

## 7. 变更记录

| 日期 | 变更 |
|------|------|
| 2026-07-26 | 创建基础版本，基于现有 docs/ 与 openspec/config.yaml 沉淀 AI 行为规范。 |
| 2026-07-26 | 技术决策确认：语言 GDScript、数据格式 Godot Resource(.tres)、测试 M0/M1 暂不引入（M2 起 GUT）。同步更新 §3.2。 |
| 2026-07-27 | 新增 `BACKLOG.md` 轻量任务收集箱；补充 §4.5 收集箱定位与升级为 OpenSpec change 的约定，必读文档表登记。 |
| 2026-07-27 | 新增 §3.5 操作边界：文本/代码由 AI 落地，引擎内操作、美术资源、UI 调整、引擎配置由用户手动操作，AI 给分步建议。 |
