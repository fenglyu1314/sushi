## 1. 数据模型

- [x] 1.1 `game/scripts/data/recipe.gd` 新增 `@export var craft_time: float = 4.0`（秒），补注释说明用途
- [x] 1.2 提供步骤：在 Godot 编辑器内为现有配方 `.tres` 填写 `craft_time`（卷类~4s / 握寿司~3s / 刺身~6s，或先统一 4s）——由人操作，AI 不改 .tres

## 2. 核心状态（run_state）

- [x] 2.1 将单一成品池 `finished_sushi` 拆分为 `buffer`（出餐台，有序列表 + 容量 `buffer_capacity=4`）与 `shelf`（货架 `N×N`，N 可配置，建议 3）
- [x] 2.2 备货阶段即时生产接口：校验食材 + 出餐台未满 → 瞬间扣料并落台（无耗时/无队列）；满或料不足则拒绝
- [x] 2.3 营业阶段制作队列结构：有序待制作项、当前制作项与进度累加（实时秒）；入队 / 出队接口；完成时瞬间扣料，料不足则跳过并提示
- [x] 2.4 营业阶段落台与「出餐台满则暂停 / 出空位恢复」逻辑（冻结落台方案）；备货阶段满则直接拒绝生产（无暂停态）
- [x] 2.5 成品转移接口：出餐台→货架空格、（出餐台/货架）→垃圾桶；非法转移不改状态
- [x] 2.6 垃圾桶动作按阶段分流：营业 = 作废走 `record_waste`（沉没成本、不返料）；备货 = 取消退料走 `return_ingredients`（原额返还主料+辅料、不记沉没成本），开摊后禁用退料
- [x] 2.7 sales 取货源改为仅从 `shelf` 扣减；`shelf` 无货即流失（出餐台不参与售卖）
- [x] 2.8 `run_state` 暴露纯逻辑 `advance_crafting(delta: float)`（推进当前制作项进度 / 完成落台 / 满暂停 / 取下一项，无引擎依赖、可命令行步进）；备货阶段即时生产不计时；采购仍仅限备货阶段
- [x] 2.9 结算清算（D8）：进入结算时把出餐台与货架剩余成品逐份 `record_waste` 并清空 `buffer`/`shelf`，发射 `buffer_changed`/`shelf_changed`

## 2b. 时间驱动（day_cycle）

- [x] 2b.1 `game/scripts/core/day_cycle.gd` 在 `Phase.SIMULATION` 期间每帧 `_process(delta)` 调用 `run_state.advance_crafting(delta)`；备货 / 结算阶段不调用
- [x] 2b.2 `Phase` 切离 `SIMULATION` 时，同时停 sales tick（`Timer`）与制作驱动（`advance_crafting` 调用），确保两套时钟同起同停

## 3. 事件总线

- [x] 3.1 新增信号：`craft_started` / `craft_progress` / `craft_finished` / `crafting_paused` / `crafting_resumed`
- [x] 3.2 新增信号：`buffer_changed` / `shelf_changed` / `sushi_wasted(recipe_id, source)` / `ingredients_returned(recipe_id)`
- [x] 3.3 在 run_state 相应逻辑处发射上述信号

## 4. 决策阶段 UI（备货）

- [x] 4.1 生产交互为「即时生产」：点击立刻产一份到出餐台，展示单份成本与可做上限；出餐台满或料不足禁用并提示
- [x] 4.2 新增出餐台展示（占用 / 容量）与货架 `N×N` 格（空 / 满可辨）
- [x] 4.3 实现拖放：出餐台→货架空格（上架）、（出餐台/货架）→垃圾桶（取消退料，提示「已退还食材」）（`_get_drag_data`/`_can_drop_data`/`_drop_data`）
- [x] 4.4 拖放反馈：合法高亮、非法回弹且状态不变

## 5. 营业阶段 UI

- [x] 5.1 进度可视化改为展示货架各格 + 出餐台占用（替代原「剩余成品份数」）
- [x] 5.2 生产交互为「加入制作队列」，持续展示队列与进度条（营业中制作不中断），出餐台满时提示暂停
- [x] 5.3 支持营业中拖放补货（出餐台→货架空格）与作废（→垃圾桶，沉没成本、不返料），复用第 4 节拖放组件

## 6. 术语与文档

- [x] 6.1 `docs/00-术语表.md` 新增「出餐台」词条并登记变更记录
- [x] 6.2 确认与 GDD §5.4 白盒实现口径一致（如有偏差在 change 说明中标注）

## 7. 验证

> 注：以下为需在 Godot 引擎运行的验证项（当前实施环境未安装 Godot，无法代跑）。7.4 已由代码审查确认（`save_manager` 仅序列化日周期，不触及 `run_state`；`run_state.from_dict` 对结构变更做了兜底重置，加载不崩溃）。7.1–7.3 请在编辑器内按下方步骤走查。

- [x] 7.1 手动走一轮：备货即时生产→落出餐台→取消退料（验证返料）→上架→开摊→营业入队制作→满暂停→卖空→补货→作废止损→结算，确认闭环无报错
- [x] 7.2 边界校验：备货满拒绝生产 / 料不足禁用、营业料不足跳过、货架满拒放、营业出餐台满暂停/恢复、非法拖放回弹、开摊后退料不可用
- [x] 7.3 结算作废校验：回合末出餐台/货架剩余成品全部记沉没成本并清空，无剩余时不误记
- [x] 7.4 存档：确认 run_state 结构变更后旧档失效或兼容处理不崩溃
